% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential.R
\name{sgcbContrast}
\alias{sgcbContrast}
\title{Contrast-defined deployment wrapper for SGCB}
\usage{
sgcbContrast(
  counts,
  sample_data,
  group_col,
  contrast_levels,
  sample_subset = NULL,
  alpha = 0.1,
  use_manifold_test = TRUE,
  bootstrap = FALSE,
  n_boot = 200,
  min_count = 10,
  min_samples = 2,
  config = NULL,
  ...
)
}
\arguments{
\item{counts}{Count matrix (genes x samples)}

\item{sample_data}{Sample metadata with one row per sample}

\item{group_col}{Column in \code{sample_data} defining the grouping factor}

\item{contrast_levels}{Character vector of length 2 giving the levels to compare, in control-then-treatment order}

\item{sample_subset}{Optional logical, numeric, or character subset applied before the contrast is formed}

\item{alpha}{FDR threshold, default 0.1}

\item{use_manifold_test}{Whether to enable manifold distance test, default TRUE}

\item{bootstrap}{Whether to compute calibrated bootstrap CIs, default FALSE}

\item{n_boot}{Number of bootstrap replicates, default 200 (only used when bootstrap=TRUE)}

\item{min_count}{Minimum count threshold, default 10}

\item{min_samples}{Minimum number of samples, default 2}

\item{config}{SGCBConfig configuration object (optional)}

\item{...}{Backward-compatible parameters that are currently accepted but not used by the current implementation}
}
\value{
An object of class \code{SGCBResults} with additional attributes describing the wrapper call
}
\description{
Subsets a user-specified two-level comparison from \code{sample_data} and
forwards the resulting count matrix and group vector to \code{sgcbDE()}.
This helper does not fit covariates and does not alter the two-group
inferential core.
}
\examples{
\donttest{
set.seed(1)
counts <- matrix(rnbinom(300 * 9, mu = 150, size = 8), nrow = 300, ncol = 9)
colnames(counts) <- paste0("sample_", seq_len(9))
sample_data <- data.frame(
  condition = c(rep("A", 3), rep("B", 3), rep("C", 3)),
  row.names = colnames(counts)
)
res_ab <- sgcbContrast(counts, sample_data, group_col = "condition", contrast_levels = c("A", "B"))
}
}
