% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dreg.R
\name{sgcbDReg}
\alias{sgcbDReg}
\title{GG Distributional Regression}
\usage{
sgcbDReg(
  counts,
  design = NULL,
  group = NULL,
  contrast = NULL,
  design_disp = NULL,
  contrast_disp = NULL,
  alpha = 0.05,
  min_count = 10,
  min_samples = 2,
  ...
)
}
\arguments{
\item{counts}{Integer count matrix (genes x samples), with rownames as gene IDs.}

\item{design}{Design matrix (samples x p). If NULL, constructed from \code{group}.}

\item{group}{Factor or character vector of group labels.}

\item{contrast}{Numeric contrast vector of length p for DE test.}

\item{design_disp}{Dispersion design matrix (samples x q) for DV test.
If NULL, only V1 (DE) is run. If \code{"auto"}, uses the same design as
\code{design}.}

\item{contrast_disp}{Dispersion contrast vector of length q for DV output.}

\item{alpha}{Significance level for \code{padj}/\code{padj_dv}.}

\item{min_count}{Minimum count threshold for gene filtering.}

\item{min_samples}{Minimum number of samples exceeding \code{min_count}.}

\item{...}{Additional arguments forwarded to the C++ engines.}
}
\value{
A \code{data.frame} (class \code{SGCBDRegResults}) with DE columns
and optionally DV columns (\code{pvalue_dv}, \code{padj_dv},
\code{dv_log2ratio}, \code{lr_stat_dv}).
}
\description{
Fits a Generalized Gamma GLM with design matrix support for differential
expression (DE) and optionally differential variability (DV) analysis.
}
\details{
V1 (mean-only): \code{log(beta_gi) = x_i' b_g}, alpha/gamma gene-specific.
V2 (mean + dispersion): additionally fits \code{log(alpha_gi) = w_i' d_g}
and tests DV via likelihood ratio.
}
